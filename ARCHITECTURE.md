# Architecture Overview

This document provides visual representations of the Faceless Video Generator architecture.

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                              CLIENT                                  │
│                         (HTTP Requests)                              │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             │ REST API
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          API SERVICE (Go)                            │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────────────┐  │
│  │  Handlers   │  │   Routes     │  │    Validation            │  │
│  │  (Chi)      │──│   (Chi)      │──│    & Business Logic      │  │
│  └─────────────┘  └──────────────┘  └──────────────────────────┘  │
└──────────────┬──────────────────────────────────┬───────────────────┘
               │                                   │
               │ Write State                       │ Enqueue Jobs
               ▼                                   ▼
    ┌──────────────────────┐          ┌──────────────────────┐
    │   PostgreSQL         │          │   Redis Queue        │
    │   (Supabase)         │          │                      │
    │  ┌────────────────┐  │          │  ┌────────────────┐ │
    │  │  projects      │  │          │  │ generate_plan  │ │
    │  │  clips         │  │          │  │ process_clip   │ │
    │  │  assets        │  │          │  │ render_final   │ │
    │  │  jobs          │  │          │  └────────────────┘ │
    │  │  presets       │  │          └──────────────────────┘
    │  └────────────────┘  │                     │
    └──────────────────────┘                     │ Dequeue Jobs
                                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        WORKER SERVICE (Go)                           │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │              Pipeline Orchestrator                          │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │    │
│  │  │ Plan Gen     │  │ Process Clip │  │ Render Final │    │    │
│  │  │ Handler      │  │ Handler      │  │ Handler      │    │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘    │    │
│  └────────────────────────────────────────────────────────────┘    │
└───────┬──────────────────┬──────────────────┬──────────────────────┘
        │                  │                  │
        │                  │                  │
        ▼                  ▼                  ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  OpenAI     │    │  Cartesia   │    │   Gemini    │
│  GPT-4      │    │    TTS      │    │   Image     │
│             │    │             │    │    Gen      │
└─────────────┘    └─────────────┘    └─────────────┘
        │                  │                  │
        │                  │                  │
        └──────────────────┴──────────────────┘
                           │
                           │ Assets
                           ▼
                 ┌──────────────────┐
                 │  Local FFmpeg    │
                 │  (Rendering)     │
                 └─────────┬────────┘
                           │
                           │ Upload Assets
                           ▼
                 ┌──────────────────┐
                 │ Supabase Storage │
                 │  (MP3, PNG, MP4) │
                 └──────────────────┘
```

## Data Flow - Video Generation

```
1. CREATE PROJECT
   Client → API → Database (project record)
                → Queue (generate_plan job)

2. GENERATE PLAN
   Queue → Worker → OpenAI (get clip structure)
                  → Database (clip records)
                  → Storage (plan.json)
                  → Queue (process_clip jobs × N)

3. PROCESS EACH CLIP (Parallel)
   Queue → Worker → Cartesia (generate audio)
                  → Storage (audio.mp3)
                  → Database (update clip)
                  → Gemini (generate image)
                  → Storage (image.png)
                  → Database (update clip)
                  → FFmpeg (render clip)
                  → Storage (clip.mp4)
                  → Database (update clip)

4. RENDER FINAL (When all clips done)
   Worker → FFmpeg (concatenate clips)
          → Storage (final.mp4)
          → Database (update project)

5. COMPLETED
   Client → API → Database (get final URL)
                → Storage (signed download URL)
```

## Database Schema Relationships

```
┌─────────────────────┐
│      series         │
│ (future feature)    │
└──────┬──────────────┘
       │
       │ has default
       │
       ▼
┌─────────────────────┐
│  graphics_presets   │
│                     │
│  - style_json       │
│  - prompt_addition  │
└──────┬──────────────┘
       │
       │ used by
       │
       ▼
┌─────────────────────┐         ┌─────────────────────┐
│     projects        │────────▶│      assets         │
│                     │         │                     │
│  - topic            │         │  - type             │
│  - status           │         │  - storage_path     │
│  - target_duration  │         │  - storage_bucket   │
└──────┬──────────────┘         └─────────────────────┘
       │                                    ▲
       │ has many                           │
       │                                    │ stored in
       ▼                                    │
┌─────────────────────┐                    │
│       clips         │────────────────────┘
│                     │
│  - clip_index       │         ┌─────────────────────┐
│  - script           │         │        jobs         │
│  - image_prompt     │         │                     │
│  - status           │◀────────│  - type             │
│  - audio_asset_id   │         │  - status           │
│  - image_asset_id   │         │  - error_message    │
│  - clip_video_id    │         └─────────────────────┘
└─────────────────────┘
```

## Job Processing Flow

```
┌────────────────────────────────────────────────────────────────┐
│                      JOB LIFECYCLE                              │
└────────────────────────────────────────────────────────────────┘

    ┌─────────┐
    │ QUEUED  │  Job created in database
    └────┬────┘  Job enqueued in Redis
         │
         │ Worker dequeues
         ▼
    ┌─────────┐
    │ RUNNING │  Worker picks up job
    └────┬────┘  Updates started_at
         │
         │ Execute job logic
         │
         ├─── Success ───┐
         │                │
         │                ▼
         │          ┌───────────┐
         │          │ SUCCEEDED │  Updates finished_at
         │          └───────────┘  Enqueue dependent jobs
         │
         └─── Failure ───┐
                         │
                         ▼
                    ┌─────────┐
                    │ FAILED  │  Updates error_message
                    └─────────┘  Updates finished_at
                                 Increments attempts
```

## Worker Pipeline Stages

```
┌────────────────────────────────────────────────────────────────┐
│                    STAGE A: GENERATE PLAN                       │
│                                                                 │
│  Input: Project (topic, duration)                              │
│  Process: OpenAI structured output                             │
│  Output: N clips with scripts, prompts, voice instructions     │
│  Time: ~10-30 seconds                                          │
└────────────────────────────────────────────────────────────────┘
                              │
                              │ Create clip records
                              │ Enqueue N process_clip jobs
                              ▼
┌────────────────────────────────────────────────────────────────┐
│                   STAGE B: PROCESS CLIPS (×N)                   │
│                       (Parallel Execution)                      │
│                                                                 │
│  ┌──────────────────────────────────────────────────────┐     │
│  │  Step 1: Generate Audio (Cartesia TTS)              │     │
│  │  - Input: script + voice_style_instruction           │     │
│  │  - Output: audio.mp3, duration_ms                    │     │
│  │  - Time: ~5-15 seconds                               │     │
│  └──────────────────────────────────────────────────────┘     │
│                           │                                     │
│                           ▼                                     │
│  ┌──────────────────────────────────────────────────────┐     │
│  │  Step 2: Generate Image (Gemini)                     │     │
│  │  - Input: image_prompt + graphics_preset             │     │
│  │  - Output: image.png                                 │     │
│  │  - Time: ~10-30 seconds                              │     │
│  └──────────────────────────────────────────────────────┘     │
│                           │                                     │
│                           ▼                                     │
│  ┌──────────────────────────────────────────────────────┐     │
│  │  Step 3: Render Clip (FFmpeg)                        │     │
│  │  - Input: image.png + audio.mp3                      │     │
│  │  - Output: clip.mp4                                  │     │
│  │  - Time: ~5-10 seconds                               │     │
│  └──────────────────────────────────────────────────────┘     │
│                                                                 │
│  Total per clip: ~20-55 seconds                                │
│  All clips process in parallel                                 │
└────────────────────────────────────────────────────────────────┘
                              │
                              │ When ALL clips rendered
                              │ Enqueue render_final job
                              ▼
┌────────────────────────────────────────────────────────────────┐
│                   STAGE C: RENDER FINAL                         │
│                                                                 │
│  Input: All clip.mp4 files (ordered by clip_index)            │
│  Process: FFmpeg concatenate                                    │
│  Output: final.mp4                                             │
│  Time: ~10-20 seconds                                          │
└────────────────────────────────────────────────────────────────┘
                              │
                              │ Upload to storage
                              │ Update project status
                              ▼
                         COMPLETED! 🎉
```

## Concurrency Model

```
┌─────────────────────────────────────────────────────────────────┐
│                  WORKER CONCURRENCY                              │
└─────────────────────────────────────────────────────────────────┘

API Service                    Worker Service (Goroutines)
────────────                   ──────────────────────────────

HTTP Server                    Queue Processor #1 ───┐
    │                              │                  │
    │                              ├─ generate_plan   │
    │                              ├─ process_clip    │
    │                              └─ render_final    │
    │                                                  │
    │                          Queue Processor #2 ───┤
    │                              │                  │
    │                              ├─ generate_plan   │
    ▼                              ├─ process_clip    ├─ MAX_CONCURRENT_JOBS
Enqueue Jobs                       └─ render_final    │
    │                                                  │
    │                          Queue Processor #3 ───┤
    │                              │                  │
    │                              ├─ generate_plan   │
    │                              ├─ process_clip    │
    │                              └─ render_final    │
    │                                                  │
    │                          Queue Processor #N ───┘
    │
    ▼
Redis Queue
    │
    ├── queue:generate_plan (FIFO)
    ├── queue:process_clip  (FIFO)
    └── queue:render_final  (FIFO)
```

## Asset Storage Structure

```
Supabase Storage Bucket: faceless-videos
│
├── {project_id_1}/
│   ├── plan.json                    # Generated plan
│   ├── clip_0_audio.mp3             # Clip 0 audio
│   ├── clip_0_image.png             # Clip 0 image
│   ├── clip_0.mp4                   # Clip 0 video
│   ├── clip_1_audio.mp3
│   ├── clip_1_image.png
│   ├── clip_1.mp4
│   ├── ...
│   └── final.mp4                    # Complete video
│
├── {project_id_2}/
│   ├── plan.json
│   ├── clip_0_audio.mp3
│   └── ...
│
└── ...
```

## Error Handling Flow

```
┌────────────────────────────────────────────────────────────────┐
│                      ERROR SCENARIOS                            │
└────────────────────────────────────────────────────────────────┘

Job Execution
     │
     ├─── Success ───────────────────┐
     │                                │
     │                                ▼
     │                        Mark job SUCCEEDED
     │                        Continue pipeline
     │
     └─── Failure ───────────────────┐
                                     │
                                     ▼
                             Mark job FAILED
                             Store error_message
                             Increment attempts
                             ┌───────────────┐
                             │ DO NOT DELETE │
                             │  PRIOR WORK   │
                             └───────────────┘
                                     │
                                     │
                         ┌───────────┴───────────┐
                         │                       │
                         ▼                       ▼
                  Update clip             Update project
                  status = failed         status = failed
                  error_message           error_message
                         │                       │
                         └───────────┬───────────┘
                                     │
                                     ▼
                             Job record persists
                             Assets preserved
                             Database intact
                             ┌───────────────┐
                             │  DEBUGGABLE   │
                             │  via API      │
                             └───────────────┘
```

## Security Boundaries

```
┌─────────────────────────────────────────────────────────────────┐
│                       SECURITY LAYERS                            │
└─────────────────────────────────────────────────────────────────┘

External Network
      │
      │ HTTPS (in production)
      ▼
┌──────────────────┐
│   Load Balancer  │  ← Rate limiting (future)
└────────┬─────────┘
         │
         │ HTTP
         ▼
┌──────────────────┐
│   API Service    │  ← Input validation
│                  │  ← Request logging
└────────┬─────────┘
         │
         ├─────────────────────┐
         │                     │
         ▼                     ▼
┌─────────────────┐   ┌─────────────────┐
│   PostgreSQL    │   │   Redis Queue   │
│   (Supabase)    │   │                 │
│                 │   │   No auth       │
│   Service Key   │   │   Local only    │
└─────────────────┘   └─────────────────┘
         │
         │ Separate credentials
         │
         ▼
┌─────────────────┐
│  Supabase       │
│  Storage        │
│                 │
│  Service Key    │
│  (write access) │
└─────────────────┘
         │
         │ Public read or signed URLs
         ▼
    End Users
```

## Scalability Path

```
V1 (Current)
────────────
Single server
API + Worker combined
~10-15 videos/hour

        │
        │ Scale vertically
        ▼

V1.5 - Optimized
────────────────
Increase MAX_CONCURRENT_JOBS
Better resource allocation
~30-50 videos/hour

        │
        │ Scale horizontally
        ▼

V2 - Distributed
────────────────
API service (multiple instances)
Worker service (multiple instances)
Shared PostgreSQL + Redis
~100-200 videos/hour

        │
        │ Add queue prioritization
        ▼

V3 - Enterprise
────────────────
API cluster (auto-scaling)
Worker pool (auto-scaling)
Redis cluster
Read replicas
~1000+ videos/hour
```

## Development vs Production

```
DEVELOPMENT                      PRODUCTION
───────────                      ──────────

Local Docker Compose             Cloud deployment
  ├── PostgreSQL (local)           ├── Managed PostgreSQL (Supabase)
  ├── Redis (local)                ├── Managed Redis (ElastiCache/etc)
  └── API+Worker (Docker)          ├── API Service (Cloud Run/ECS)
                                   └── Worker Service (Cloud Run/ECS)

Single instance                  Multiple instances
No SSL                          SSL/TLS everywhere
Sample API keys                 Production API keys
Debug logging                   Structured logging
No monitoring                   Full monitoring
                                   ├── Health checks
                                   ├── Metrics
                                   ├── Alerts
                                   └── Log aggregation
```

---

This architecture enables:
- ✅ Independent scaling of API and Workers
- ✅ Fault tolerance (job state persisted)
- ✅ Debuggability (full job history)
- ✅ Concurrent processing (multiple clips at once)
- ✅ Flexibility (easy to add new services)
- ✅ Cost efficiency (pay for what you use)
